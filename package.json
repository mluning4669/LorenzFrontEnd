{
  "name": "nodeclient",
  "version": "1.0.0",
  "description": "web app to consume end point that supplies a base 64 encoded string",
  "main": "server.js",
  "dependencies": {
    "request": "^2.81.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Mike Luning",
  "license": "UNLICENSED"
}
